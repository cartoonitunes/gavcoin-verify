# Research Notes

## Bytecode Analysis

### Function Selectors Decoded

All 11 function selectors in the runtime bytecode have been identified:

```
0x06005754 = name()                              — from 'named' base contract
0x1fa03a2b = isApprovedFor(address,address)       — from BasicCoin
0x673448dd = isApproved(address)                  — from BasicCoin
0x67eae672 = sendCoinFrom(address,uint256,address)— from BasicCoin
0x99f4b251 = mine()                               — from GavCoin
0xa550f86d = named(bytes32)                       — from 'named' base contract
0xa6f9dae1 = setOwner(address)                    — from 'owned' base contract
0xbbd39ac0 = coinBalanceOf(address)               — from BasicCoin
0xc86a90fe = sendCoin(uint256,address)             — from BasicCoin
0xd26c8a8a = coinBalance()                        — from BasicCoin
0xdaea85c5 = approve(address)                     — from BasicCoin
```

### Constructor Behavior

The creation bytecode does the following:
1. PUSHes "GavCoin" (32 bytes, zero-padded): `7f476176436f696e00...`
2. PUSHes NameReg address: `73084f6a99003dae6d3906664fdbf43dd09930d0e3`
3. Calls `NameReg.register("GavCoin")` via selector `e1fa8e84`
4. Sets `owner = msg.sender` (from `owned`)
5. Sets `m_balances[owner] = tota` (from `coin` — likely 0)
6. Sets `m_lastNumberMined = block.number`
7. Copies runtime bytecode to memory and returns it

### Storage Layout

Based on inheritance order `BasicCoin, named("GavCoin"), coin("GAV", 1000), owned`:
- Slot 0: `m_balances` mapping (from BasicCoin)
- Slot 1: `m_approved` mapping (from BasicCoin)
- Slot 2: `owner` (from owned)
- Slot 3: `m_lastNumberMined` (from GavCoin)

Note: `sym`, `denom`, `tota` from the `coin` base contract may or may not occupy storage slots depending on how the preprocessor inlined them. If they're constructor-only values not stored, they won't appear in storage.

Verified on-chain: slot 3 contains the last mined block number (confirmed in the GavCoin dapp).

### The #require Preprocessor

The `#require` directive was part of the Mix IDE and Mist browser's Solidity tooling. It worked similarly to C's `#include` — it resolved contract names from a standard library and inlined them before compilation.

Sources:
- Standard library location: `ethereum/mix` repo, `stdc/` directory
- Contracts available: `owned`, `mortal`, `Config`, `NameReg`
- NOT found in standard library: `named`, `coin`, `service` — these may have been in a different location or generated by the preprocessor itself

### The `named` Contract

Reconstructed from bytecode:
- Constructor calls `NameReg(0x084f...).register(_name)` 
- `name()` returns the NameReg address (acts as a getter for the registry)
- `named(bytes32)` calls `NameReg.addressOf(bytes32)` to resolve names

### The `coin` Contract

Reconstructed from source analysis:
- Takes `(symbol, denomination)` parameters
- Sets variables: `sym`, `denom`, `tota`
- `tota` is referenced in GavCoin constructor as initial balance (likely 0)

### Key Challenges

1. **Preprocessor reconstruction**: The exact output of `#require` is unknown. The inlining order, whitespace, and structure may affect compilation.
2. **Compiler version**: 17 candidate versions (0.1.1 through 0.3.6). Each may produce different bytecode.
3. **Optimization**: Unknown whether optimization was enabled.
4. **Base contract structure**: `named` and `coin` are reconstructions, not originals.
5. **Solidity language changes**: Early versions had `hash()` instead of `sha3()`, `string32` vs `bytes32`, and other differences that changed between minor versions.

### Approach

1. Try compiling the flattened source with each candidate compiler version
2. Compare output bytecode with on-chain bytecode
3. Iterate on the flattened source structure based on compilation errors and partial matches
4. Use the creation bytecode for exact matching (includes constructor + runtime)

## Compilation Results (March 3, 2026)

### Key Finding: Partial Match Achieved

Using solc 0.2.0 optimized, the first 79 characters match the on-chain creation bytecode exactly:
```
0x60606040527f476176436f696e00000000000000000000000000000000000000000000000000
```

This confirms:
1. Constructor pushes "GavCoin" first (named contract runs before coin/owned)
2. Optimization was enabled
3. The source structure is approximately correct

### Remaining Differences

**Size gap:** Compiled = 2430 chars, On-chain = 2210 chars (110 bytes too large)

**Divergence at char 79:** The on-chain code uses PUSH20 (address) immediately after "GavCoin", while our compiled output uses PUSH32 (selector) first. This is a code generation ordering difference.

**Storage layout confirmed:** On-chain uses slot 2 for `owner`, matching `BasicCoin, named, owned` without any `coin` state variables. The `coin` base contract either contributes no state or doesn't exist as a separate contract.

**Constructor `m_balances[owner] = tota`**: Since there's no `tota` state variable on-chain, this line was either:
- Dead code (tota=0, so it's a no-op and the optimizer removed it)
- A compile-time constant from the preprocessor
- Simply not present in the actual compiled source

### Versions Tested

All versions from 0.1.6 through 0.3.6 compile. Optimized output sizes:
- 0.1.6/0.2.0: 2430-2500 chars (closest to target 2210)
- 0.3.x: 2746-2754 chars (further from target)

**Best candidate: solc 0.2.0 optimized** — closest size match and identical prefix.

### Next Steps

1. The 110-byte gap likely comes from our `named` contract implementation being larger than the original
2. Need to find/reconstruct the exact preprocessor output for `named()`
3. The code generation ordering difference (PUSH20 vs PUSH32 first) may indicate a different compiler version or source structure
4. Try removing the name() getter and using a simpler implementation
5. Check if the named(bytes32) getter was implemented differently (inline assembly?)
